"use client";
import { FormEvent, useEffect, useState } from "react";
import Link from "next/link";
import { onAuthStateChanged, User } from "firebase/auth";
import { doc, onSnapshot, setDoc } from "firebase/firestore";
import { ArrowLeft, LoaderCircle, Save } from "lucide-react";
import { useRouter } from "next/navigation";
import { auth, db, firebaseReady } from "@/lib/firebase/client";
export default function SettingsPage() { const router = useRouter(); const [user, setUser] = useState<User | null>(null); const [ready, setReady] = useState(false); const [emails, setEmails] = useState(""); const [busy, setBusy] = useState(false); const [message, setMessage] = useState(""); const [error, setError] = useState(""); useEffect(() => { if (!auth) return; return onAuthStateChanged(auth, (next) => { setUser(next); setReady(true); }); }, []); useEffect(() => { if (ready && !user) router.replace("/control-room?next=/control-room/settings"); }, [ready, user, router]); useEffect(() => { if (!db || !user) return; return onSnapshot(doc(db, "siteSettings", "notifications"), (snapshot) => setEmails(Array.isArray(snapshot.data()?.notificationEmails) ? snapshot.data()?.notificationEmails.join(", ") : "")); }, [user]); async function save(event: FormEvent) { event.preventDefault(); if (!db) return; setBusy(true); setError(""); setMessage(""); try { const list = emails.split(",").map((email) => email.trim()).filter((email) => email.includes("@")); await setDoc(doc(db, "siteSettings", "notifications"), { notificationEmails: list }, { merge: true }); setMessage("Notification recipients saved."); } catch (saveError) { setError(saveError instanceof Error ? saveError.message : "Could not save settings."); } finally { setBusy(false); } } if (!firebaseReady || !ready || !user) return <main className="login-page"><LoaderCircle className="spin" size={28} /></main>; return <main className="admin-page"><section className="admin-content"><Link className="admin-back" href="/control-room"><ArrowLeft size={16} /> Back to Control Room</Link><div className="admin-topbar"><div><p className="admin-kicker">Settings</p><h1>Email notifications</h1><p className="admin-intro">Choose who receives new prayer requests and testimony submissions.</p></div></div>{message && <div className="admin-notice success">{message}</div>}{error && <div className="admin-notice error">{error}</div>}<form className="sermon-form settings-form" onSubmit={save}><div className="upload-title"><Save size={19} /><div><h2>Submission recipients</h2><p>Separate multiple email addresses with commas.</p></div></div><label>Email addresses<input value={emails} onChange={(event) => setEmails(event.target.value)} placeholder="pastor@example.com, admin@example.com" /></label><button className="admin-button" disabled={busy}>{busy ? "Saving..." : "Save recipients"}</button></form></section></main>; }
