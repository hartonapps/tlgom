import type { Metadata } from "next";
import GalleryBrowser from "@/components/GalleryBrowser";
export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }): Promise<Metadata> { const { slug } = await params; const name = slug.split("-").map((part) => part.charAt(0).toUpperCase() + part.slice(1)).join(" "); const title = `${name} | Photo Gallery | The Life Global Outreach Ministries`; const description = `Explore photographs from ${name} at The Life Global Outreach Ministries.`; return { title, description, alternates: { canonical: `/photo-gallery/${slug}` }, openGraph: { title, description, url: `/photo-gallery/${slug}`, type: "website", images: [{ url: "/logo.png", width: 512, height: 512, alt: `${name} gallery` }] }, twitter: { card: "summary_large_image", title, description, images: ["/logo.png"] } }; }
export default async function GalleryAlbumPage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; return <GalleryBrowser slug={slug} />; }
